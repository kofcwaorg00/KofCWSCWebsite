@model IdentityUserRole<string>

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4></h4>
    <hr />
    <dl class="row">
    </dl>

    <form asp-action="Delete" asp-route-RoleID="@Model.RoleId" asp-route-UserID="@Model.UserId">
        <dl class="row">
            @*<dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.RoleId)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.UserId)
            </dd>*@
            <dt class="col-sm-2">
                Role To Delete
            </dt>
            <dd class="col-sm-10">
                @ViewBag.RoleToDelete
            </dd>

            
        </dl>
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index" asp-route-id="@ViewBag.KofCMemberID">Back to List</a>
    </form>
</div>
